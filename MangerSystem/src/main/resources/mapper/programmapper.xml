<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="site.part.ms.mapper.ProgramMapper">
    <select id="paging" resultType="java.util.List">
    	select * from programs limit #{startindex},#{count}
    </select>
</mapper>